# Project workflow

- After completing a requested code change and its relevant validation, commit only the task-related files and push the finished change directly to `origin/main`, unless the user explicitly requests a different branch or asks not to push.
- Never include unrelated modified or untracked files in those commits.
